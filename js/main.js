"use strict";

/* ---------------------------------------------------------------------
 * The GTD roster is closed (500/500 claimed) and minting is live on
 * OpenSea, so this site no longer collects entries -- it's just a
 * status page pointing people to the collection. If you ever need to
 * point the "Mint on OpenSea" links somewhere else, update the href
 * values directly in index.html (there's no backend/config left here).
 * ------------------------------------------------------------------- */
